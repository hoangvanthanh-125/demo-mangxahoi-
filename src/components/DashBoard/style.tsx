import { makeStyles } from "@material-ui/core";

export default makeStyles(() => ({
  header:{
    position:'fixed',
    left:0,
    right:0,
    top:0,
    zIndex:1
  },
  children:{
    marginTop:90
  }
}))